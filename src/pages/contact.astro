---
import BaseLayout from "../layouts/BaseLayout.astro";
import { SITE_TITLE, NAV_LINKS } from '../resources';

const aboutLink = NAV_LINKS.find(link => link.href === "/contact");
const pageTitle = `${aboutLink?.label ?? "Contact"} - ${SITE_TITLE}`;

---

<BaseLayout title={pageTitle}>
    <h3>Custom instruments</h3>
    <p>I’m based in Toronto and I love experimenting when making <span class="dotted-underline" title="/ˈbuːb.nɨ/, plural form of buben">bubny</span> and accessories—each one is a bit different, and that’s part of the fun. If you're looking for an instrument to play or want something made just for you, feel free to reach out.</p>
    <form action="https://formkeep.com/f/2642501b0de0"
        accept-charset="UTF-8"
        enctype="multipart/form-data"
        method="POST">
        <input type="hidden" name="utf8" value="✓">

        <div>
            <label for="Name">Name</label>
            <input type="text" id="name" name="name" required>
        </div>

        <div>
            <label for="email-address">Email Address</label>
            <input type="email" id="email-address" name="email" required>
        </div>

        <div>
            <label for="message">Message</label>
            <textarea id="message" name="message" rows="4" required></textarea>
        </div>

        <button type="submit">Submit</button>
    </form>
</BaseLayout>

<style>
    form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        max-width: 400px;
        margin: 3rem auto;
    }

    label {
        font-weight: bold;
        margin-bottom: 0.5rem;
        text-align: left;
        display: block;
    }

    input, textarea {
        width: 100%;
        padding: 0.5rem;
        border: 1px solid #ccc;
        border-radius: 4px;
        font-size: 1rem;
    }

    button {
        padding: 0.75rem;
        background-color: #007BFF;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 1rem;
        width: 100px;
    }

    button:hover {
        background-color: #0056b3;
    }
</style>